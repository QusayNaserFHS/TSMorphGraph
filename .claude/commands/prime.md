---
model_set: low
---
# Prime

Quick codebase orientation — read project config and summarize structure, stack, and commands.

## Workflow

1. Read `CLAUDE.md` and summarize: project purpose, tech stack, key commands, project structure.
2. If `CLAUDE.md` is missing, read `package.json` and list the `src/` directory instead.
3. Report a concise summary of the project and how to use it.
